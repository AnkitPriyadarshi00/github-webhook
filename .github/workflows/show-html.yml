name: Show HTML file on push to main

on:
  push:
    branches:
      - main    # trigger only on pushes to main branch

jobs:
  show-html:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Display contents of index.html
        run: |
          echo "----- index.html content start -----"
          cat index.html
          echo "----- index.html content end -----"
