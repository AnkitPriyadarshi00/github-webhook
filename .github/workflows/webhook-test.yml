name: Webhook Test

on:
  push:
    branches:
      - main  # or the branch you want to monitor

jobs:
  run-on-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Print a message
      run: echo "GitHub push event detected! Commit message: ${{ github.event.head_commit.message }}"

    # You can add more steps here like calling your local API or running scripts
